/**
 * Props for the LayerToggle map control.
 */
export interface LayerToggleProps {
  /** Additional CSS class applied to the root element. */
  className?: string;
  /**
   * Whether the panel starts open. Defaults to `false`.
   */
  defaultOpen?: boolean;
}
