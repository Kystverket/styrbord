import { AlertLevel, Width } from './alert.types';
export declare const getIcon: (level: AlertLevel) => React.JSX.Element;
export declare const alertStyle: (width: Width, level: AlertLevel) => string;
